version: 0.2
phases:
  install:
    commands:
      - aws cloudformation package --template-file samTemplate.yaml --s3-bucket lambda-deployement-pipeline-bucket --output-template-file outputTemplate.yaml
artifacts:
  type: zip
  files:
    - search-photos.yaml
    - outputTemplate.yaml
